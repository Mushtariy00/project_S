# 바이브코딩 CI/CD 기초편 - 프롬프트 모음

수강생 배포용. 아래 코드블록을 복사해 Claude Code 입력창에 붙여넣기.

## 사용법

> 절 번호 `[8-1]` 은 **프롬프트 순서**입니다. 슬라이드 소제목 번호(`8-2` 등)와는 별개입니다.

1. 이 파일을 **VS Code** 로 열기.
2. `Ctrl + Shift + V` 로 미리보기를 켜면 코드블록 오른쪽 위에 복사 버튼 표시.
3. 필요한 블록을 복사해 Claude Code 입력창에 붙여넣고 Enter.

> PowerShell 붙여넣기는 **마우스 우클릭** 또는 **Ctrl + V**.
> 여러 줄을 붙여넣어도 Enter 입력 전에는 실행되지 않음.

## 세션을 열 때마다

폴더 이름은 장마다 다릅니다. 5·7장 `claude-test`, 8장 `taskflow-simple`, 9~11장 `taskflow-pro`.

폴더를 처음 만들 때 (5·7장 예시)

```powershell
D:
mkdir D:\claude-test
cd D:\claude-test
claude
```

이미 있는 폴더로 돌아올 때

```powershell
D:
cd D:\claude-test
claude -c
```

> 새 폴더에서는 `claude` (옵션 없이), 이어서 할 때는 `claude -c`.
> 폴더 이름은 장에 맞게 바꿉니다. 8장은 `taskflow-simple`, 9~11장은 `taskflow-pro`.

Claude Code 안에서

```text
/model sonnet
```

```text
/effort low
```

---

## 5장 - CLAUDE.md 연습용 (앱은 만들지 않음)

작업 폴더: `D:\claude-test`

> 5장에서는 파일만 만듭니다. git 으로 올리는 절차는 7장에서 진행합니다.
> 아래 블록을 그대로 Claude Code 에 붙여넣습니다. 앱 코드는 만들지 않습니다.

```text
아래 내용을 이 폴더 루트에 CLAUDE.md 라는 이름으로 저장해줘.

## 연습용 - 실제 구현은 하지 않음

## 기술 스택
- 서버: FastAPI + Python 3.11 + SQLite
- 프론트: Vanilla JS + Tailwind CDN
- 폴더: backend/ 와 frontend/ 분리

## 코딩 규칙
- 한국어 주석 사용
- 변수명: snake_case (Python), camelCase (JS)
- 모든 API는 /api/ 경로 prefix 사용

## 금지 사항
- jQuery 사용 금지
- CSS 직접 작성 금지 (Tailwind만 사용)
```

---

## 7장 - Git & GitHub

작업 폴더: `D:\claude-test` (5장에서 만든 폴더 그대로)

> 아래는 Claude Code 안에서 한국어로 지시하는 방식입니다.
> 파워셸에 직접 치는 방법은 슬라이드 왼쪽 창을 참고하세요.

> 아이디와 이메일은 각자 GitHub 계정으로 바꿔서 씁니다.

### [7-5] git 로컬 설정 + 첫 커밋  (1줄)

```text
git 로컬로 설정해줘. 이름은 BulNim, 메일은 kimkap10@gmail.com. 그리고 커밋해줘.
```

### [7-5] 커밋 확인  (1줄)

```text
커밋 이력 보여줘
```

### [7-6] GitHub 저장소 연결 + push  (1줄)

```text
깃허브 저장소 https://github.com/BulNim/claude-test.git 에 연결해줘. 거부되면 덮어써도 돼.
```

> 원격에 README 커밋이 있으면 `rejected (fetch first)` 로 거부됨.
> `--force` 로 덮어쓰면 원격의 README 는 사라짐.

### [7-8] 실습용 .env 만들기  (1줄)

```text
실습용으로 .env 파일을 만들어서 API_KEY=dummy 한 줄 넣고 커밋해줘.
```

### [7-8] .gitignore 생성  (1줄)

```text
앞으로 올리면 안 되는 것들이 안 올라가게 해줘.
```

### [7-8] 이미 커밋된 .env 빼기  (1줄)

```text
이 키가 저장소 어디에도 남지 않게 해줘.
```

### [7-8] 원격 상태 확인  (1줄)

```text
이 키가 지금도 어딘가에서 되살아날 수 있는지 확인해줘.
```

> 최신 커밋에서 빠져도 과거 커밋에는 그대로 남음.
> 한 번이라도 올라간 키는 폐기하고 새로 발급받을 것.

### [7-10] 전체 환경 점검  (1줄)

```text
지금 이 환경 점검해줘. 버전 표로 보여주고 실제로 올릴 수 있는 상태인지도 확인해줘.
```

> `claude` 는 버전만 표에 나옴. 최신 여부는 `claude update` 로 따로 확인.

### [7-10] 빠진 것 확인  (1줄)

```text
빠진 게 있으면 설치 방법, 없으면 없다고 알려줘
```

---

## 8장 - 심플 바이브

작업 폴더: `D:\taskflow-simple` (반드시 새 빈 폴더)

> Claude Code 안이면 `/exit` 로 나온 뒤, 파워셸에서 아래를 실행합니다.

```powershell
D:
mkdir D:\taskflow-simple
cd D:\taskflow-simple
claude
```

### [8-1] 앱 만들기  (1줄)

```text
업무 관리 앱 만들어줘. 업무 추가, 삭제, 상태 변경 기능이 있어야 해.
```

> 이 한 줄이 전부. 기술 스택도 DB 도 폴더 구조도 지시하지 않음.
> 결과가 사람마다 다르게 나오는 것이 정상. 옆 사람과 스택부터 다른 그 차이를 확인하는 것이 이 실습의 목적.
> 9장에서 스펙 문서를 갖추면 그때부터 결과 고정.

### [8-2] 실행  (1줄)

```text
방금 만든 앱을 실제로 실행하고 접속 주소를 알려줘.
```

> 의존성 설치 · 가상환경 · 서버 기동은 AI 가 스택에 맞게 알아서 처리.

### [8-3] 동작 확인  (1줄)

```text
추가, 상태 변경, 삭제 세 가지 되는지 확인하고 결과 보여줘. 끝나면 서버 종료해줘.
```

> 서버가 한 번에 안 꺼질 수 있음. 셸만 닫으면 뒤에서 돌던 프로세스가 포트를 계속 물고 있음.
> 다음 실습에서 주소가 안 열리면 아래 한 줄로 다시 확인.

### [8-3] 포트가 아직 물려 있을 때  (1줄)

```text
아직 그 포트 물고 있는 게 있는지 보고 있으면 끝내줘.
```

### [8-4] git 설정 + 커밋 + GitHub push  (3줄)

```text
git 로컬로 설정해줘. 이름은 BulNim, 메일은 kimkap10@gmail.com.
.gitignore 만들고 simple vibe done 으로 커밋해줘.
깃허브 저장소 https://github.com/BulNim/taskflow-simple.git 에 연결해줘.
```

> 저장소 주소의 계정 이름은 각자 것으로 바꿔서 씁니다.
> .gitignore 에 넣을 항목은 AI 가 이 앱의 스택을 보고 고름.

### [8-4] 커밋 확인  (1줄)

```text
커밋 이력 보여줘
```

### [8-5] README.md 작성 + push  (1줄)

```text
이 앱 설명서를 README.md 로 만들어서 올려줘.
```

> 저장소는 GitHub 웹에서 `taskflow-simple` 이라는 이름으로 미리 만들어 둡니다.
> 9장 결과(`taskflow-pro`)와 나란히 두고 비교하기 위한 기록입니다.

> 비교할 것 네 가지: 언어와 프레임워크, 저장 방식(DB · 파일 · 메모리),
> 파일 개수, 웹 화면인지 CLI 인지. 옆 사람과 맞춰 보세요.

---

## 9 ~ 11장 - TaskFlow Pro

작업 폴더: `D:\taskflow-pro` (반드시 새 빈 폴더)

> Claude Code 안이면 `/exit` 로 나온 뒤, 파워셸에서 아래를 실행합니다.

```powershell
D:
mkdir D:\taskflow-pro
cd D:\taskflow-pro
claude
```

### [9-1] CLAUDE.md 작성  (17줄)

```text
현재 폴더에서 풀스택 웹 앱을 본격 구조로 만든다.
CLAUDE.md부터 작성해. 아래 4개 섹션 그대로:

1) 너의 역할: 10년차 시니어 풀스택, 유지보수 우선,
   한국어 응답, 식별자는 영어
2) 기술 스택 (고정 - 임의 변경 금지):
   - 백엔드 backend/ : FastAPI + Python 3.11 이상 + SQLite
   - 프론트 frontend/ : Vanilla JS + Tailwind CDN,
     index.html 과 app.js 2개 파일만
   - 모든 API 경로는 /api/ 접두사, 테스트는 pytest
3) 작업 시작 전 절차: docs/ 아래 6개 파일을 이 이름·이 순서로 읽기
   00-overview.md  01-product.md  02-specs.md
   03-design.md  04-tasks.md  05-conventions.md
   (이 6개는 지금 만들지 않는다. 이름만 기록)
4) 절대규칙 6개: 추측 금지 / 돌발 의존성 금지 / 테스트 없이 완료 금지 /
   시크릿 하드코딩 금지 / 폴더 구조 임의 변경 금지 /
   docs 와 어긋나는 지시를 받으면 구현 전에 문서명과 조항을 들어 되물을 것

이번 단계에서는 CLAUDE.md 한 개만 만든다.
```

### [9-2] docs/ 6개 파일 + 00-overview.md  (12줄)

```text
docs/ 폴더에 아래 6개 파일을 이 이름 그대로 만든다.
다른 파일은 추가하지 말 것:
  00-overview.md   01-product.md   02-specs.md
  03-design.md     04-tasks.md     05-conventions.md

내용은 00-overview.md 에만 채운다. 나머지 5개는 빈 파일로만 만든다.
그리고 CLAUDE.md 에 적힌 docs 파일명·순서가 방금 만든 6개와
같은지 확인하고, 다르면 CLAUDE.md 를 이 6개 이름으로 고쳐라.

00-overview.md 내용:
- 프로젝트 한 줄: TaskFlow Pro - 팀 업무 관리 풀스택 웹 앱
- 6개 파일 매핑표 (파일명 | 역할 | 알 수 있는 것)
- 읽는 순서: 00 > 01 > 02 > 03 > 04 > 05
- 왜 이렇게 나누는가 - 관심사 분리 원칙으로 설명
```

### [9-3] 01-product.md  (WHY)  (18줄)

```text
docs/01-product.md 만 작성해. 다른 docs 파일은 건드리지 말 것.
다음 내용으로:
- 목표: 팀 업무 시각화. '지금 누가 뭐 해?'가 사라지게
- 페르소나: 10명 규모 스타트업 팀리더 (30~40대)
- MVP 범위:
  - CRUD 4종 모두 화면에서 - 추가/목록/수정/삭제
  - 상태 분류 + 마감 시각 (due_at) 지정, 표시
  날짜만이 아니라 시간까지 (예: 2026-12-31 18:00)
  - 라이트/다크 테마 토글 (localStorage)
  - 모바일 반응형 (360px)
- UI 톤: Mac OS 스타일
  둥근 모서리, 부드러운 그림자, 반투명 카드, 시스템 폰트
- 확장: JWT 로그인, 팀, Kanban, 채팅, CI/CD
- 범위 외: 외부결제, 네이티브앱, WebRTC, 외부캘린더, 파일업로드
  WebSocket - MVP는 폴링. 확장 단계 보류
- 성공 기준 (실습에서 눈으로 확인 가능한 것만):
  새로고침해도 데이터 유지 / 360px 에서 안 깨짐
  CRUD 4종 화면 동작 / 테마 토글 작동
```

### [9-4] 02-specs.md  (WHAT)  (21줄)

```text
02-specs.md 작성해. 다음 내용으로 고정:
- Task 모델 필드 7개 (이 순서, 타입까지 그대로):
  id (INTEGER, PK, AUTOINCREMENT)
  title (VARCHAR 200, 필수)
  description (TEXT, 선택)
  status (todo/in_progress/done, 기본값 todo)
  due_at (DATETIME UTC, 선택)
  created_at, updated_at (DATETIME, 서버 자동)
- 검증:
  title/status/due_at 형식 위반 > 400 / 없는 id > 404
  스펙에 없는 필드가 오면 422 로 거부 - 조용히 무시 금지
  due_at 은 UTC 로 저장하고 화면에서 로컬로 변환
  응답의 날짜 세 필드는 UTC ISO 8601 로 통일
- REST API 5개, 경로는 /api/ 접두사 필수:
  POST 201 / GET 목록 200 / GET 단건 200 / PUT 200 / DELETE 204
  목록엔 description 제외, 단건은 포함
- 화면 명세 (화면별 표 1개씩 총 4개):
  추가 - 폼 (title / due_at / status)
  목록 - 카드 (status 배지 + 마감까지 남은 시간)
  수정 - 카드 클릭 > 모달 (전 필드 수정 가능)
  삭제 - 휴지통 > 확인 > DELETE
```

### [9-5] 03-design.md  (HOW)  (15줄)

```text
03-design.md 작성해. 표 1개(8행)로 정리.
열은 선택 / 대안 / 근거 / 트레이드오프 4개 고정.
아래 8개 항목을 이 번호·이 이름 그대로:

1) 백엔드 - FastAPI / 대안 Django, Express
2) 프론트 - Vanilla JS + Tailwind CDN / 대안 React, Vue
   프론트는 백엔드가 같은 오리진에서 제공. file:// 로 직접 열지 않음
3) DB - SQLite (SQLAlchemy ORM, 추후 PostgreSQL 전환 고려)
   / 대안 PostgreSQL 즉시 도입
4) CSS - Tailwind만. styled-components 금지
5) 실시간 - MVP 는 폴링 3초. WebSocket 은 확장 단계 보류
6) 상태관리 - 모듈 변수 + DOM 직접 갱신
7) 디자인 시스템 - Mac OS UI 톤 / 대안 Material, Ant
8) 테마 - 라이트/다크 토글, localStorage, 초기값은 시스템 설정

마지막에 의존성 추가 정책: 이 문서에 사유를 적기 전에는 도입 불가.
사전 승인 목록에 httpx 를 적을 것 (테스트 구동에 필요).
```

### [9-6] 04-tasks.md  (구현 순서)  (12줄)

```text
04-tasks.md 작성해. MVP를 3개 Phase로 진행:
- Phase 1 (설계): CLAUDE.md + docs/ 6종 작성 - 지금 완료
- Phase 2 (백엔드): backend/ FastAPI > CRUD API 5개 > Swagger 확인
- Phase 3 (프론트): frontend/ HTML+JS+Tailwind > 화면 > API 연결 > git push

Phase 마다 체크리스트를 표로 만들 것. 단계 수는 이대로 고정:
  Phase 1 - 10단계 / Phase 2 - 10단계 / Phase 3 - 8단계
열은 단계 / 검증 방법 / 완료 3개. 완료 열은 [ ] 체크박스로 두고
진행하면서 [x] 로 바꾼다.
Phase 2 의존성은 fastapi, uvicorn, sqlalchemy, pytest, httpx 로 한정.

Phase 이름과 개수는 위 3개 그대로 고정. 변경 금지.
이후 'backend 진행해' = Phase 2 전체, 'frontend 진행해' = Phase 3 전체.
진행 규칙: 순서대로만, 병렬 금지, 단계별 검증 필수.
```

### [9-7] 05-conventions.md + git 첫 커밋  (23줄)

```text
05-conventions.md 작성해. 다음 내용으로:
- 명명: 백엔드 snake_case, 프론트 camelCase, 컴포넌트 PascalCase
  식별자는 영어, 주석만 한국어
- 금지 5개 (금지/이유/대안 3열):
  print 디버깅 / 노이즈 / logging 모듈
  bare except / 예외 삼킴 / except SpecificError
  비밀번호 하드코딩 / 보안사고 / .env + os.getenv
  any 타입(TS) / 의미 상실 / 명시적 타입
  !important / 우선순위 꼬임 / 셀렉터 개선
- .gitignore 에 넣을 것: __pycache__/, .venv/, *.db, *.log
- 테스트 매트릭스 (표: 케이스 | 요청 | 기대 응답):
  정상 생성 | POST title 만 | 201
  목록 | GET /api/tasks | 200, description 없음
  단건 | GET /api/tasks/{id} | 200, description 있음
  수정 | PUT 전 필드 | 200   /   삭제 | DELETE | 204
  title 누락 | 400   /   status 오값 | 400
  due_at 형식 오류 | 400   /   없는 id | 404
  스펙 외 필드 | 422
- git 커밋 규칙: feat/fix/docs/refactor/test/chore + 한국어 요약

끝나면 git 로컬로 설정해줘. 이름은 BulNim, 메일은 kimkap10@gmail.com.
.gitignore 도 만들고 첫 커밋: 'docs: Phase 1 설계 문서 7종 작성'
깃허브 저장소 https://github.com/BulNim/taskflow-pro.git 에 연결해줘.
거부되면 덮어써도 돼.
```

### [10-1] 백엔드 시작  (1줄)

```text
backend 진행해.
```

### [10-2] 서버 실행 + Swagger  (1줄)

```text
서버 띄우고 Swagger 열어줘.
```

> 실행 명령 · 폴더 · 포트 · 주소는 `04-tasks.md` Phase 2 와 `03-design.md` 에 있음.
> 서버는 11장 실습이 끝날 때까지 켜 둘 것. Swagger 화면은 각자 브라우저에서 확인.

### [10-2] 서버 종료  (1줄)

```text
서버 종료해줘.
```

> 한 번에 안 꺼질 수 있음. `--reload` 로 띄우면 프로세스가 둘이라 하나만 지우면 다시 살아남.
> 주소가 계속 열리면 아래 한 줄을 다시 넣을 것.

### [10-2] 확인  (1줄)

```text
잘 떴네
```

### [10-3] 테스트 매트릭스 검증  (1줄)

```text
05-conventions.md 테스트 매트릭스대로 검증해.
```

> 몇 개를 어떻게 검증할지는 `05-conventions.md` 에 이미 적혀 있습니다.
> docs 에 있는 것을 프롬프트에 다시 적지 않습니다. 그것이 docs 를 두는 이유입니다.

### [10-4] 절대규칙 시연 - 일부러 의존성 요청  (1줄)

```text
날짜 처리 라이브러리 추가해서 created_at 포맷팅 개선해줘
```

### [10-4] 절대규칙 시연 - 선택 응답  (1줄)

```text
표준 라이브러리로 처리해. 날짜 형식은 02-specs 대로 전부 통일해줘.
```

> 형식이 그대로 보이면 서버를 다시 띄운 뒤 확인.

### [10-5] 백엔드 완료 + 커밋  (1줄)

```text
backend 완료 처리하고 커밋한 뒤 올려줘.
```

> 체크박스 갱신은 `04-tasks.md`, 커밋 메시지 규칙은 `05-conventions.md` 에 있음.
> 그 둘은 AI 답변 로그에 나올 내용이지 사람이 치는 말이 아님.

### [11-1] 프론트엔드 시작  (1줄)

```text
frontend 진행해.
```

### [11-2] 서버 다시 띄우고 화면 열기  (1줄)

```text
서버 다시 띄우고 화면 열어줘.
```

> `frontend/` 가 생긴 뒤라 정적 파일 마운트를 다시 잡아야 하므로 껐다 켬.
> 종료 절차는 위 [10-2] 서버 종료와 같음. `file://` 로 직접 열지 않음 (03-design).

### [11-2] 확인  (1줄)

```text
잘 떴네
```

### [11-3] 엔드포인트 동작 확인  (1줄)

```text
02-specs 대로 엔드포인트 동작 다 확인해.
```

> 엔드포인트 개수와 목록 · 단건 응답 차이는 `02-specs.md` 에 있음.

### [11-4] MVP 성공 기준 검증  (1줄)

```text
01-product MVP 성공 기준 검증해.
```

> 360px 화면과 테마 토글은 각자 브라우저에서 직접 확인.

### [11-5] Phase 3 완료 + git push  (1줄)

```text
Phase 3 완료 처리하고 커밋한 뒤 올려줘.
```

> 체크박스 갱신과 기본 브랜치 확인은 `04-tasks.md` 대로 AI 가 처리.
> 원격은 9장에서 이미 연결해 둔 상태.

### [11-5] README.md 작성 + push  (1줄)

```text
이 앱 설명서를 README.md 로 만들어서 올려줘. docs 와 어긋나지 않게.
```
